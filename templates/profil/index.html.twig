{% extends 'base.html.twig' %}

{% block title %}Hello ProfilController!{% endblock %}

{% block body %}
  {% if app.user %} 
          <div class="rounded-top text-white d-flex flex-row" style="background-color: #000; height:200px;">
            <div class="ms-4 mt-5 d-flex flex-column" style="width: 150px;">
              <img style=" width : 80px ; margin : 10px ;  width : 100px; " src="img/{{asset(app.user.image)}}"
                alt="Generic placeholder image" class="img-fluid img-thumbnail mt-4 mb-2"
                style="width: 150px; z-index: 1">
                
             
             
              
              <div>
              
              </div>

            </div>
            <div class="ms-3" style="margin-top: 130px;">
            <div>
              <h3 style="margin: 220 "> {{app.user.username}}</h3>
              <p> {{app.user.adresse}}</p>
              </div>
            </div>
          </div>
          <div class="p-4 text-black" style="background-color: #f8f9fa;">
            <div class="d-flex justify-content-end text-center py-1">
            
          </div>
          <div class="card-body p-4 text-black">
            <div class="mb-5">
              <p class="lead fw-normal mb-1">Detail personnelle</p>
              <div class="p-4" style="background-color: #f8f9fa;">
                <p class="font-italic mb-1">Email : {{app.user.email}}</p>
                <p class="font-italic mb-1">Numero : {{app.user.numero}}</p>
                <p class="font-italic mb-0">Adresse : {{app.user.adresse}} </p>
              </div>
            </div>
            <div class="d-flex justify-content-between align-items-center mb-4">
              <p class="lead fw-normal mb-0">liste des demandes</p>
              <p class="mb-0"><a href="#!" class="text-muted">Consulter tous</a></p>
            </div>
            <div class="row g-2">
              
<table  class="table table-striped">
<tr >
<th>EmailEtudiant </th>
<th>EcolePartanaire </th>
<th>Departement</th>
<th>MoyenneG3eme</th>
<th>MoyenneG4eme</th>
<th>score </th>
<th>etat </th>

</tr>

{% for i in em %}
{% if i.EmailEtudiant ==  app.user.email %}
<tr>
<td> {{i.EmailEtudiant}} </td>
<td> {{i.EcoleP}} </td>
<td> {{i.Departement}} </td>
<td> {{i.MoyenneG3eme}} </td>
<td> {{i.MoyenneG4eme}} </td>
<td> {{i.score }} </td>
<td> {{i.etat }} </td>



</tr>
{% endif %}
{% endfor %}


</table>
            </div>
          </div>
       

  {% endif %}
{% endblock %}
